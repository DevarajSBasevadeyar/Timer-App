# Timer-App
A live clock app that displays the current time, date, and timezone with automatic updates every second using JavaScript's setInterval. Features a modern glassmorphism design with a purple gradient background and responsive layout.
